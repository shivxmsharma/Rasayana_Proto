// Web stub for react-native-maps
// This prevents native-only modules from being imported on web

const MapView = () => null;
const Marker = () => null;
const Circle = () => null;

export default MapView;
export { Marker, Circle };
